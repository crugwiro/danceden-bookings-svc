spring.application.name=bookings

# Postgres not set up yet - re-enable once the DB exists (see BookingsApplication's
# @SpringBootApplication exclude, which also needs to be removed at that point)
#spring.datasource.url=jdbc:postgresql://localhost:5432/bookingsdb
#spring.datasource.username=postgres
#spring.datasource.password=postgres
#spring.datasource.driver-class-name=org.postgresql.Driver

#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
