services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: bookingsdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - bookings-postgres-data:/var/lib/postgresql/data

volumes:
  bookings-postgres-data:
